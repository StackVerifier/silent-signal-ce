import { Analytics } from '@vercel/analytics/next'
import type { Metadata, Viewport } from 'next'
import { Inter, JetBrains_Mono } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
})

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
})

export const metadata: Metadata = {
  title: 'Silent Signal — Release Intelligence',
  description: 'Operational intelligence platform for Jira release risk analysis. Rule-based engineering intelligence without AI.',
  generator: 'v0.app',
  keywords: ['jira', 'release management', 'sprint intelligence', 'qa monitoring', 'risk analysis'],
}

export const viewport: Viewport = {
  colorScheme: 'dark',
  themeColor: '#070B18',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable} bg-[#070B18]`}>
      <body className="antialiased font-sans">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
